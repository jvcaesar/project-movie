import React from 'react'

export const InfoScreen = ({ empty }) => {
  return (
    <div className='info-screen'>
        No movies to display. Select from one of the options above.
    </div>
  )
}
